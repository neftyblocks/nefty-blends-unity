using UnityEngine;

public class CraftAssetPopupController : MonoBehaviour
{
    // This controller tracks what is the current selected blend id inside the crafting menu.
    [SerializeField] public int currentBlendId { get; set; }
}
